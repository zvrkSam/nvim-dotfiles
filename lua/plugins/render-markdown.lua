-- add this to autocmds.lua if you want it be toggled off by default
-- require("render-markdown").toggle()

return {
  "MeanderingProgrammer/markdown.nvim",
  enabled = false,
  opts = {
    heading = {
      enabled = false,
    },
  },
}
