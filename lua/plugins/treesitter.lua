-- UNTIL Kanagawa gets updated
-- This is only a temperary fix
-- If you want Kanagawa theme for markdown enable treesitter code

return {
  -- "nvim-treesitter/nvim-treesitter",
  -- opts = function(_, opts)
  --   opts.ensure_installed = vim.tbl_filter(function(i)
  --     return not vim.tbl_contains({ i }, "markdown")
  --   end, opts.ensure_installed)
  -- end,
}
